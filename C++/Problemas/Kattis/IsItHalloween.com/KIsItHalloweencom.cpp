#include <bits/stdc++.h>
#define endl "\n"
using namespace std;

int main(){

    string in; getline(cin, in);
    cout << (((in == "OCT 31") || (in == "DEC 25")  ) ? "yup\n" : "nope\n");

    return 0;
}
