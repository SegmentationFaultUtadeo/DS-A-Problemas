#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int main(){

    string in1; getline(cin, in1);
    while (!in1.empty()){
        cout << in1 << endl;
        getline(cin, in1);
    }

    return 0;
}
